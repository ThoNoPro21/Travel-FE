import { getArticleType } from '@/src/types/Article';
import { RootState } from '../store';



// export const selectCountPostPending = (state: RootState, payload: number): number | undefined => {
//     const result = state.dataArticle.listPost.map((article: getArticleType) => article.articles_id === parseInt(payload,10));
//     return result;
// };
